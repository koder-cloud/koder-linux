" Koder Linux vim defaults

" Syntax highlighting
syntax on

" Case insensitive search
set ignorecase
set smartcase

" Return cursor to last position when reopening a file
if has("autocmd")
  au BufReadPost * if line("'\"") > 0 && line("'\"") <= line("$")
    \| exe "normal! g`\"" | endif
endif

" Mouse support: select, copy, paste from clipboard
set mouse=a
if has('clipboard')
  set clipboard=unnamedplus
endif
