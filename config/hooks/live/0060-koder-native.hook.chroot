#!/bin/sh
set -e

echo "Building koder-native (kterm)..."

cd /usr/src/koder-native

# Build with meson
meson setup build --prefix=/usr
meson compile -C build
meson install -C build

# Clean up source
rm -rf /usr/src/koder-native

echo "koder-native installed."
